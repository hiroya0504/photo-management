<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter
        xmlns="https://github.com/spotbugs/filter/3.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://github.com/spotbugs/filter/3.0.0 https://raw.githubusercontent.com/spotbugs/spotbugs/master/spotbugs/etc/findbugsfilter.xsd">

    <!--
      SpotBugs is overly cautious about constructor injection and exposing
      Spring-managed beans. In a DI container these references are owned by Spring,
      not by application code, so defensive copying does not apply.
    -->
    <Match>
        <Class name="~com\.example\.photomanagement\..*"/>
        <Bug pattern="EI_EXPOSE_REP,EI_EXPOSE_REP2"/>
    </Match>

    <!--
      Beans that validate their @ConfigurationProperties in the constructor
      intentionally fail fast when misconfigured (e.g. JWT secret too short).
      CT_CONSTRUCTOR_THROW does not apply to that pattern.
    -->
    <Match>
        <Class name="~com\.example\.photomanagement\..*"/>
        <Bug pattern="CT_CONSTRUCTOR_THROW"/>
    </Match>
</FindBugsFilter>
